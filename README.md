# 💼 AI Job Recommender System
**Capstone Rekayasa Data 2026 — Universitas Siliwangi**

Sistem rekomendasi lowongan kerja berbasis **Hybrid RAG** (Retrieval-Augmented Generation) yang menggabungkan:
- **SBERT** untuk pencarian semantik berbasis vektor
- **Google Gemini** sebagai lapisan presentasi percakapan

---

## 🏗️ Arsitektur Sistem

```
User Input (Natural Language)
        │
        ▼
┌───────────────────────┐
│   Gemini (Extractor)  │  → Ekstrak: skills, lokasi, gaji, remote
└───────────────────────┘
        │
        ▼
┌───────────────────────┐
│   SBERT Vector Search │  → Cosine Similarity (70% bobot)
└───────────────────────┘
        │
        ▼
┌───────────────────────┐
│  Filtering & Ranking  │  → Gaji (15%) + Lokasi/Remote (15%)
└───────────────────────┘
        │
        ▼
┌───────────────────────┐
│  Gemini (Humanizer)   │  → Rangkum hasil jadi jawaban natural
└───────────────────────┘
        │
        ▼
     Output Chat
```

### Logika Pembobotan
| Faktor | Bobot | Jenis |
|--------|-------|-------|
| Skill Similarity (SBERT) | **70%** | Soft Score |
| Kesesuaian Gaji | **15%** | Bonus Score |
| Lokasi & Remote | **15%** | Hard Filter |

---

## 📁 Struktur Folder

```
ai_job_recommender/
├── app.py                    # Aplikasi Streamlit utama
├── requirements.txt          # Dependensi Python
├── .env.example              # Template API Key
├── .env                      # API Key Anda (buat sendiri)
│
├── data/
│   └── job_data.csv   # Dataset 260.000 lowongan
│
├── models/
│   └── job_embeddings.pkl    # Generated by preprocess.py
│
└── src/
    ├── __init__.py
    ├── preprocess.py         # Tahap offline: buat embeddings
    ├── recommender.py        # Core engine: SBERT + ranking
    └── gemini_layer.py       # Integrasi Gemini API
```

---

## ⚡ Cara Menjalankan

### 1. Install Dependensi

```bash
pip install -r requirements.txt
```

### 2. Siapkan API Key Gemini

Buat file `.env` di root folder:
```
GEMINI_API_KEY=AIza_your_key_here
```

Dapatkan API key gratis di: https://aistudio.google.com/app/apikey

### 3. Jalankan Preprocessing (SEKALI SAJA)

```bash
python src/preprocess.py
```

> Proses ini akan membuat file `models/job_embeddings.pkl` (~15-20 MB).
> Waktu sekitar 2-5 menit tergantung CPU.

### 4. Jalankan Aplikasi

```bash
streamlit run app.py
```

Buka browser ke: **http://localhost:8501**

---

## 💬 Contoh Penggunaan

| Input User | Yang Terjadi |
|-----------|-------------|
| *"Cari kerja Data Scientist di Singapore gaji minimal $120K"* | Gemini ekstrak: skills=Data Scientist, lokasi=Singapore, min_salary=120000 |
| *"Saya jago Python dan ML, mau remote"* | Gemini ekstrak: skills=Python, Machine Learning, remote_only=True |
| *"Ada lowongan Cybersecurity di London atau New York?"* | Filter multi-lokasi, skill similarity Cybersecurity |

---

## 🛠️ Tech Stack

| Komponen | Teknologi |
|----------|-----------|
| Language | Python 3.x |
| NLP Engine | `sentence-transformers` (SBERT) |
| LLM Engine | Google Gemini 1.5 Flash |
| Web UI | Streamlit |
| Vector Ops | NumPy (Cosine Similarity) |
| Data | Pandas |

---

## 📊 Dataset

- **File**: `future_jobs_dataset.csv`
- **Baris**: 10.000 lowongan
- **Kolom**: `job_id`, `job_title`, `industry`, `location`, `salary_usd`, `skills_required`, `remote_option`, `company_size`, `posting_date`
- **Lokasi**: Singapore, Tokyo, London, New York, Dubai, Berlin
- **Salary Range**: $50,013 – $249,990/tahun

---

## ⚠️ Troubleshooting

| Masalah | Solusi |
|---------|--------|
| `Engine belum siap` | Jalankan `python src/preprocess.py` |
| `GEMINI_API_KEY belum diatur` | Buat file `.env` atau masukkan di sidebar |
| `ModuleNotFoundError` | Jalankan `pip install -r requirements.txt` |
| Hasil tidak relevan | Pastikan query menggunakan bahasa Inggris untuk skill teknis |

---

*Dibuat untuk Capstone Rekayasa Data 2026 — Universitas Siliwangi*
